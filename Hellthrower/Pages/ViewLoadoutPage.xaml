<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Hellthrower.Pages.ViewLoadoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Hellthrower.Models"
    xmlns:converters="using:Hellthrower.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converters:ButtonTriggerConverter x:Key="ButtonTriggerConverter" />
        <DataTemplate x:Key="Keys" x:DataType="models:Trigger">
            <ListViewItem IsHitTestVisible="False">
                <Button IsEnabled="False" Content="{x:Bind Key, Mode=OneWay, Converter={StaticResource ButtonTriggerConverter}}" />
            </ListViewItem>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Name: </TextBlock>
        <TextBlock Grid.Row="0" 
            Grid.Column="1" 
            Text="{x:Bind ViewModel.Loadout.Name, Mode=OneWay}" />
        
        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Stratagem: </TextBlock>
        <TextBlock Grid.Row="1" 
            Grid.Column="1" 
            Text="{x:Bind ViewModel.Loadout.Stratagem, Mode=TwoWay}" />
            
        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Triggers: </TextBlock>
        <ListView 
            Grid.Row="2" 
            Grid.Column="1" 
            ItemsSource="{x:Bind ViewModel.Loadout.Keys, Mode=OneWay}"
            ItemTemplate="{StaticResource Keys}" />
    
        <Button Grid.Row="3" Grid.Column="0" Click="Delete">Delete loadout</Button>
    </Grid>
</Page>
